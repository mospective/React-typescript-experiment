import React from 'react'

export default function Arrays() {

    let fruits: string[] = ["apples", "pears", "banana"];
    let ids: number[] = [23, 45, 24];

    let mixArray: (string | number)[] = ["apple", 23, 45, "pears", 24, "banana"];

    let isTrue: boolean = true;

  return (
    <div>Arrays
        {}
    </div>
  )
}
